import React from 'react';
import ReactDOM from 'react-dom/client';
import Text from './contextAPI';
import './index.css';
import Root from './root';



const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
<Text>

    <Root/>
</Text>
      
    
            
      </React.StrictMode>
);
